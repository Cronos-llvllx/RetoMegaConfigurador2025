export interface PromotionalPackage {
        IdPromotionalPackage: number; // Clave primaria para la entidad "paquete promocional"
        IdPackage: number; // Clave foránea para la entidad "paquete"
}
