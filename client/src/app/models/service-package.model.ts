export interface ServicePackage {
  // Clave foránea que referencia al Paquete
  idPackage: number;
  // Clave foránea que referencia al Servicio
  idService: number;
  // cantidad: number; (Si la Cantidad es por Paquete, no por Servicio)

}
