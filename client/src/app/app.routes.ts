import { Routes } from '@angular/router';
import { SubscribersManagerComponent } from './components/subscribers-manager/subscribers-manager.component';

export const routes: Routes = [
    { path: '', component: SubscribersManagerComponent }
];
